# Built-in post-preproc workflow templates

This directory is intentionally empty in the public package.
Users put their own templates under
`$FMRIFLOW_HOME/addons/workflows/`.

A workflow dropped here would be the default for every fmriflow
install; that's a strong claim that no concrete workflow
currently meets. When we have one that's truly generic, it lands
here.
